[pytest]
# Put this directory (the one holding the `privacy_firewall/` package) on
# sys.path so `import privacy_firewall` works no matter how pytest is
# launched: the `pytest` console script does not add the cwd, unlike
# `python -m pytest`. Requires pytest >= 7.0.
pythonpath = .
testpaths = tests

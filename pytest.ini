[pytest]
# Repo-root config: lets `pytest` run from the repository root too.
# The project itself lives in `privacy_firewall/`; point sys.path and
# collection there. Running pytest from inside `privacy_firewall/` uses
# that directory's own pytest.ini instead.
pythonpath = privacy_firewall
testpaths = privacy_firewall/tests
